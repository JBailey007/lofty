
{{#each goals as |goal| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{render_shape}}}
      <a href="/goal/{{goal.id}}">{{goal.name}}</a>
    </h2>
  <div>
    <p>
        {{goal.description}}
    </p>
  </div>
  <button>New Goal</button>
